action_inherited();
///Trail
if (percent_chance(30))
if (abs(hsp) > 2 || abs(vsp) > 2)
{
    var a = create_effect(x, y, sprDust, random_range(0.2, 0.4));
    a.image_alpha = random_range(0.4, 1);
}


